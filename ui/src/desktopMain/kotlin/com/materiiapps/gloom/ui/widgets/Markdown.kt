package com.materiiapps.gloom.ui.widgets

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

@Composable
actual fun Markdown(
    text: String,
    modifier: Modifier
) {

    // TODO: Find some way to actually render this
    Text(
        text = text
    )
}