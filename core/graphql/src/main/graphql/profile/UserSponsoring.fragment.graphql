fragment UserSponsoring on User {
    sponsoring(first: $total, after: $cursor) {
        pageInfo {
            endCursor
            hasNextPage
            hasPreviousPage
            startCursor
        }
        totalCount
        nodes {
            ...UserListUser
            ...OrgListOrg
        }
    }
}