fragment FeedRepository on Repository {
    id
    name
    owner {
        __typename
        login
        avatarUrl
    }
    description
    stargazerCount
    primaryLanguage {
        color
        name
    }
    contributorsCount
    openGraphImageUrl
    viewerHasStarred
}