fragment FeedUser on User {
    id
    login
    name
    avatarUrl
    bio
    isFollowingViewer
    isViewer
    viewerIsFollowing
    repositories {
        totalCount
    }
    followers {
        totalCount
    }
}